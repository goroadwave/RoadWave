name: smoke-riley

on:
  workflow_dispatch:
    inputs:
      target_url:
        description: "URL to test manually"
        required: false
        default: "https://www.getroadwave.com"

jobs:
  smoke-riley:
    runs-on: ubuntu-latest

    steps:
      - name: Manual smoke-riley placeholder
        run: |
          echo "smoke-riley is now manual-only."
          echo "Target URL: ${{ github.event.inputs.target_url }}"
          echo "This workflow no longer runs automatically on push, deployment_status, pull_request, or preview deploys."
